"""Тесты GDAI (pytest).

Зачем пакет, а не просто каталог: тесты подключают общие помощники из
`conftest.py` и обязаны импортироваться однозначно, без коллизий имён модулей
(`test_env.py` в разных каталогах — классический источник «duplicate basename»).
"""
