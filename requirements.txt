# Основные зависимости GDAI.
# Зачем так мало: чем меньше внешнего кода, тем проще воспроизвести обучение.
numpy>=1.24
torch>=2.0
pygame>=2.5
matplotlib>=3.7

# --- опциональные (мягкий импорт, код обязан работать и без них) ---
# imageio>=2.31            # запись gif/mp4 демонстраций (gdai demo, viz)
# opencv-python-headless   # быстрый ресайз кадров при захвате настоящей игры
# mss                      # захват экрана (gdai.realgame.capture)
# pynput                   # эмуляция нажатий (gdai.realgame.play)

# --- для разработки ---
# pytest>=7.0
