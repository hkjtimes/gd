"""GDAI — нейросеть, которая учится проходить Geometry Dash.

Идея пакета в двух строках: `perception` переводит ЛЮБОЙ дизайн уровня в одну
и ту же каноническую семантическую карту, а `agent` учится играть только по
этой карте и потому не может переобучиться на декорации.

Зачем этот файл почти пустой: импорт `gdai` не должен тянуть torch и pygame —
физику и генерацию уровней нужно уметь запускать в самом лёгком окружении.
Тяжёлые подпакеты импортируются явно (`from gdai.agent.ppo import train_agent`).
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
